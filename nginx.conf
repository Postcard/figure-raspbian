
http {

    server {
        listen       80;
        server_name  localhost;

        location ~* \.(html|jpg|jpeg|gif|png|css|js|ico|xml)$ {
            root              /data/resources;
        }

    }

}
